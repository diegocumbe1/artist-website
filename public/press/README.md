# Press kit PDF

Cuando el PDF final este listo, guardalo en esta carpeta con este nombre:

```txt
pipe-cumbe-press-kit.pdf
```

La ruta publica quedara:

```txt
/press/pipe-cumbe-press-kit.pdf
```

Luego cambia `pressKit.pdfPath` en `src/data/site.ts` de `""` a:

```txt
/press/pipe-cumbe-press-kit.pdf
```

